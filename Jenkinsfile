pipeline {
  agent any
  stages {
    stage('Build') {
      steps {
        sh 'python --version'
      }
    }
    stage('run') {
      steps {
        sh 'echo "now is runing a pipeline"'
      }
    }
  }
}